/**
 * Author: Panagiotis NICOLAOU
 * Email: pnikola@unipi.gr
 */
dependencies {
}

task copyTask(type: Copy) {
    from "build/libs/container-manager-${version}.jar"
    into '../artifacts'
//    rename "container-manager-${version}.jar", 'container-manager.jar'
}

build.dependsOn copyTask
