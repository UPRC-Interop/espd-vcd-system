/**
 * Author: Panagiotis NICOLAOU
 * Email: pnikola@unipi.gr
 */
dependencies {
}

task copyTask(type: Copy) {
    from "build/libs/in-memory-db-${version}.jar"
    into '../artifacts'
//    rename "in-memory-db-${version}.jar", 'in-memory-db.jar'
}