dependencies {
}

task copyTask(type: Copy) {
    from "build/libs/user-manager-${version}.jar"
    into '../artifacts'
//    rename "user-manager-${version}.jar", 'user-manager.jar'
}

build.dependsOn copyTask
