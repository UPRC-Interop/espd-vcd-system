/**
 * Author: Panagiotis NICOLAOU
 * Email: pnikola@unipi.gr
 */
dependencies {
    compile project(':schema')

    /* Java Transaction API */
    compile 'javax.transaction:jta:1.1'

    /* postgresql */
    //noinspection GroovyAssignabilityCheck
    compile libraries.postgresql

    /* h2 */
    //noinspection GroovyAssignabilityCheck
    compile libraries.h2
}

task copyTask(type: Copy) {
    from "build/libs/model-${version}.jar"
    into '../artifacts'
//    rename "model-${version}.jar", 'model.jar'
}

build.dependsOn copyTask