import {NumberOjsValidationDirective} from './number-ojs-validation.directive';

describe('NumberOjsValidation Directive', () => {
  it('should create an instance', () => {
    const directive = new NumberOjsValidationDirective();
    expect(directive).toBeTruthy();
  });
});
