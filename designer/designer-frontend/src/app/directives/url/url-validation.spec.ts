import {UrlValidationDirective} from './url-validation.directive';

describe('UrlValidation Directive', () => {
  it('should create an instance', () => {
    const directive = new UrlValidationDirective();
    expect(directive).toBeTruthy();
  });
});
