import {VatNumberValidationDirective} from './vat-number-validation.directive';

describe('VatNumberValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new VatNumberValidationDirective();
    expect(directive).toBeTruthy();
  });
});
