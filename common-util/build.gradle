/**
 * Author: Panagiotis NICOLAOU
 * Email: pnikola@unipi.gr
 */
dependencies {
}

task copyTask(type: Copy) {
    from "build/libs/common-util-${version}.jar"
    into '../artifacts'
//    rename "common-util-${version}.jar", 'common-util.jar'
}

build.dependsOn clean, copyTask