/**
 * Author: Panagiotis NICOLAOU
 * Email: pnikola@unipi.gr
 */
dependencies {
}

task copyTask(type: Copy) {
    from "build/libs/compiler-${version}.jar"
    into '../artifacts'
//    rename "compiler-${version}.jar", 'compiler.jar'
}

build.dependsOn copyTask