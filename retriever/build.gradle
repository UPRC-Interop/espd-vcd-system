dependencies {
}

task copyTask(type: Copy) {
    from "build/libs/retriever-${version}.jar"
    into '../artifacts'
//    rename "retriever-${version}.jar", 'retriever.jar'
}

build.dependsOn copyTask
